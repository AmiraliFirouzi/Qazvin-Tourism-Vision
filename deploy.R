# Deployment helper
# Configure rsconnect once for your account, then run:
#
# install.packages("rsconnect")
# library(rsconnect)
# rsconnect::deployApp()
#
# The application directory is this project root.
